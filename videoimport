import cv2

cap = cv2.VideoCapture("Resources/passenger.mp4")
while True :
      success, img = cap.read()
      cv2.imshow("Video", img)

      if cv2.waitKey(0) & 0xff == ord('q'):

          break
